import { Helmet } from "react-helmet-async";

export default function Dashboard() {
    return (
        <div>
            <Helmet>
                <title>Jobs Fly | Dashboard</title>
            </Helmet>
            Dashboard
        </div>
    )
}
